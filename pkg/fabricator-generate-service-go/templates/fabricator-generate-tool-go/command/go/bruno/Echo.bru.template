!!delimiters <<< >>>
meta {
  name: Echo
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/Echo
  body: json
  auth: bearer
}

headers {
  x-eos-user: {{playerID}}
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
   "content": "to echo"
  }
}
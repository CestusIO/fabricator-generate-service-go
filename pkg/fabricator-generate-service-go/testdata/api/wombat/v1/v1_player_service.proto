
syntax = "proto3";

package wombat.v1;
option go_package = "example.com/api/wombat/v1/go/wombat_v1_player";

import "v1_common.proto";

// PlayerWombat is a Player Wombat
service PlayerWombat {
     rpc Echo(EchoMessage) returns (EchoMessage);
}
